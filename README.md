# React Stopwatch ⚛️

A stopwatch built with React using `useState` + `useRef` hooks.

## Features
- Start, Stop, Reset
- HH:MM:SS display format
- setInterval ID stored in useRef to persist across renders

## Run
```bash
npm install
npm run dev
```

## Author
ThunderOoops
